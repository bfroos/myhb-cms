{
  "collectionName": "components_blocks_reviews",
  "info": {
    "displayName": "Reviews",
    "icon": "star"
  },
  "options": {},
  "attributes": {
    "headline": {
      "type": "string",
      "required": false
    },
    "reviews": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::review.review"
    },
    "oddItemsTheme": {
      "type": "enumeration",
      "required": true,
      "default": "neutral",
      "enum": [
        "light",
        "soft",
        "neutral",
        "strong"
      ]
    },
    "evenItemsTheme": {
      "type": "enumeration",
      "required": true,
      "default": "soft",
      "enum": [
        "light",
        "soft",
        "neutral",
        "strong"
      ]
    }
  },
  "config": {}
}
